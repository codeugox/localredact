# localredact
Browser-based PII redaction for PDFs. Drop a file, review what was detected, download a clean version with sensitive information removed. Runs entirely in your browser — nothing is uploaded or transmitted.
